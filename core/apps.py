from django.apps import AppConfig


class CoreConfig(AppConfig):
    default_auto_field = "django.db.models.BigAutoField"
    name = "core"

    def ready(self):
        # Logger is now configured via Django's LOGGING settings
        # No initialization needed here
        pass
